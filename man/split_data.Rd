% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_data.R
\name{split_data}
\alias{split_data}
\title{Split Data into Training and Testing Sets}
\usage{
split_data(data, prop = 0.8)
}
\arguments{
\item{data}{A data frame containing the data to split. Must contain an 'id' column.}

\item{prop}{A numeric value between 0 and 1 indicating the proportion of data for the training set. Default is 0.8.}
}
\value{
A list containing two data frames: \code{train} and \code{test}.
}
\description{
Splits a dataset into training and testing sets based on a specified proportion.
It uses the 'id' column to ensure there is no data leakage (overlap) between the sets.
}
\examples{
df <- data.frame(id = 1:10, value = rnorm(10))
split <- split_data(df, prop = 0.8)
split$train
split$test
}
